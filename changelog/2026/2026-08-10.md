# Week of Mon Aug 10 – Fri Aug 14, 2026

1. **Cursor adapter settings sync** — [14651a5](https://github.com/guscsales/hele-skills/commit/14651a5)
   - `hele cursor` syncs `settings.agents.models` cursor keys from the template without overwriting existing cursor choices — Gustavo Sales + Claude

2. **Verify / chat report layout** — [7254323](https://github.com/guscsales/hele-skills/commit/7254323)
   - Stop VERIFY RUN reports from mangling chat layout; normalize report and approval blocks across agents and skills — Gustavo Sales + Claude
   - CI checks bundled adapter freshness and fails PRs with rebuild hints — Gustavo Sales + Claude

3. **Flow diagram — QA↔build fix loop** — [72bb154](https://github.com/guscsales/hele-skills/commit/72bb154)
   - Show the QA↔build fix loop in the canonical flow diagram; keep README / `hele ai` / agent rules in sync — Gustavo Campos Sales + Claude

4. **Docs, CHANGELOG, and release tagging** — [c9173f1](https://github.com/guscsales/hele-skills/commit/c9173f1)
   - Add `.docs/` (introduction, getting started, skills, CLI); backfill CHANGELOG and historical tags; CI tags new versions and requires CHANGELOG entries — Gustavo Campos Sales

5. **QA fixes report without re-run** — [8e13a05](https://github.com/guscsales/hele-skills/commit/8e13a05)
   - `--generate-fixes-report` reconstructs `QA_REPORT` from stub statuses, beads, and traces — Gustavo Campos Sales

6. **Chat reports without fences** — [70f7693](https://github.com/guscsales/hele-skills/commit/70f7693)
   - Skills no longer paste the chat report inside a markdown fence (agents were copying it into Cursor and gluing titles onto divider lines); each skill points at the persona or canonical block and describes the shape in prose — Gustavo Campos Sales + Claude

7. **Plan Paper/Figma artboard gate** — [73c9b99](https://github.com/guscsales/hele-skills/commit/73c9b99)
   - `/hele-plan` requires `<paper-to-code>` / `<figma-to-code>` with exact artboard ids on Van Pelt tasks; pixels from MCP `get_jsx`, not layout prose — Gustavo Campos Sales + Claude

8. **PRD markdown-inside-XML** — [df2c474](https://github.com/guscsales/hele-skills/commit/df2c474)
   - PRODUCT_DESCRIPTION template writes markdown (headings, tables, named mermaid) inside the existing XML section tags so CEOs can review in preview; agents still find `<what>` / `<business-rules>` / `<flows>`. Plans, specs, stubs, DB changes stay XML-first — Gustavo Campos Sales + Claude
